-	script	GlobalDropSystem	-1,{
OnNPCKillEvent:
    if (killedrid >= 1000) {
        if (rand(1, 10000) <= 1000) { // 10% chance
            getmapxy(.@map$, .@x, .@y, BL_PC); 
            makeitem 50000, 1, .@map$, .@x, .@y;
        }
    }
    end;
}