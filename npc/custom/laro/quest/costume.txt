//===== Hercules Script ======================================
//= Costume Convert Quest
//===== By: ==================================================
//= Mateuus
//===== Current Version: =====================================
//= 1.0
//===== Description: =========================================
// ===== Comentários Adicionais: =================================
// = 1.0 roteiro inicial.

// Shop NPCs -- supplying no argument displays entire menu.
//	callfunc "qshopQ"{,<shop ID>{,<shop ID>{,...}}};
//============================================================
prt_cas,90,285,4	script	Costume Clown#1	631,{ 

	mes "[Costume Clown]";
	if(#language == 1) {
		mes "Aqui você pode converter seus Hats em um Visual.";
		switch(select("Eu quero converter.:Não, obrigado.")) {
		case 1:
				next;
				mes "Por favor, selecione o que converter.";
				mes "Lembre-se, Cartas e Refino serão removidos.";
				next;
					setarray .@Position$[1],"Top","Mid","Low","Garment";
					setarray .@Position[1],     EQI_HEAD_TOP,    EQI_HEAD_MID,   EQI_HEAD_LOW, EQI_GARMENT;
					set .@Menu$,"";
				for( set .@i, 1; .@i < 6; set .@i, .@i + 1 )
				{
					if( getequipisequiped(.@Position[.@i]) )
					set .@Menu$, .@Menu$ + .@Position$[.@i] + "-" + "[" + getequipname(.@Position[.@i]) + "]";
					set .@Menu$, .@Menu$ + ":";
				}
				set .@Part, .@Position[ select(.@Menu$) ];
				if( !getequipisequiped(.@Part) )
				{
					mes "[Costume Clown]";
					mes "Você não está vestindo nada lá.";
					close;
				}
				
				//Pegar na Loja mais tarde
				set $ItemCostumeID, getequipid(.@Part); 
				set $ItemCostumePart, .@Part; 
				
				mes "[Clown]";
				mes "Você quer fantasiar seu " + getitemname(getequipid(.@Part)) + "?";
				next;
				if( select("Sim, prossiga:Não, eu sinto muito.") == 2 )
				{
					mes "[Costume Clown]";
					mes "Precisa de algum tempo para pensar nisso, hein?";
					mes "Tudo bem, eu posso entender.";
					close;
				}
				
				for( .@i = 0; .@i < getarraysize(.list); .@i++ ){
					if(getequipid(.@Part) == .list[.@i]){	
						callfunc "qshopQ";
						close;
					}
				}
				mes "[Costume Clown]";
				mes "Desculpe, eu não trabalho com esse item";
				close;
		case 2:
			mes "[Costume Clown]";
			mes "Muito bem. Volte imediatamente se você quiser meus serviços.";
			close;
		}
	}else{
		mes "Here you can convert your headgears into a Costume Headgear.";
		switch(select("I want to convert.:No thanks.")) {
		case 1:
				next;
				mes "Please, select what to convert.";
				mes "Remember, cards and refine will be removed.";
				next;
					setarray .@Position$[1],"Top","Mid","Low","Garment";
					setarray .@Position[1],     EQI_HEAD_TOP,    EQI_HEAD_MID,   EQI_HEAD_LOW, EQI_GARMENT;
					set .@Menu$,"";
				for( set .@i, 1; .@i < 6; set .@i, .@i + 1 )
				{
					if( getequipisequiped(.@Position[.@i]) )
					set .@Menu$, .@Menu$ + .@Position$[.@i] + "-" + "[" + getequipname(.@Position[.@i]) + "]";
					set .@Menu$, .@Menu$ + ":";
				}
				set .@Part, .@Position[ select(.@Menu$) ];
				if( !getequipisequiped(.@Part) )
				{
					mes "[Costume Clown]";
					mes "You are not wearing anything there."; 
					close;
				}
				
				//Pegar na Loja mais tarde
				set $ItemCostumeID, getequipid(.@Part); 
				set $ItemCostumePart, .@Part; 
				
				mes "[Costume Clown]";
				mes "You want to Costume your " + getitemname(getequipid(.@Part)) + "?";
				next;
				if( select("Yes, proceed:No, I am sorry.") == 2 )
				{
					mes "[Clown]";
					mes "Need some time to think about it, huh?";
					mes "Alright, I can understand.";
					close;
				}
				
				for( .@i = 0; .@i < getarraysize(.list); .@i++ ){
					if(getequipid(.@Part) == .list[.@i]){	
						dispbottom "[Costume Crowh] ItemID: "+$ItemCostumeID+".";
						callfunc "qshopQ";
						close;
					}
				}
				mes "Sorry, I do not work with this item";
				close;
		case 2:
			mes "[Costume Clown]";
			mes "Very well. Return at once if you seek my services.";
			close;
		}
	}
	
	
OnInit:
	//Lista de itens que são permitidos
	setarray .list[0],5230,5231,5232,5233;	
	questinfo 1322, QTYPE_QUEST, 1;
	end;
}

// Script Core
//============================================================
-	script	quest_shopQ	-1,{
function AddQ; function Chk; function Slot; function A_An;
OnInit:
	freeloop(1);

// -----------------------------------------------------------
//  Basic shop settings.
// -----------------------------------------------------------

	set .Announce,1;	// Anunciam busca conclusão? (1: sim / 0: nenhum)
	set .ShowSlot,1;	// Mostrar espaços de item? (2: todos os equipamentos / 1: se ranhuras> 0/0: never)
	set .ShowID,0;  	// Mostrar IDs de item? (1: sim / 0: nenhum)
	set .ShowZeny,0;	// Mostrar custo Zeny, se houver? (1: sim / 0: nenhum)
	set .MaxStack,1;	// Número máximo de itens de quest comprado ao mesmo tempo.

// -----------------------------------------------------------
//  Points variable -- optional quest requirement.
//	setarray .Points$[0],"<variable name>","<display name>";
// -----------------------------------------------------------

	setarray .Points$[0],"#CASHPOINTS","Cash Points";

// -----------------------------------------------------------
//  Shop IDs -- to add shops, copy dummy data at bottom of file.
//	setarray .Shops$[1],"<Shop 1>","<Shop 2>"{,...};
// -----------------------------------------------------------

	//setarray .Shops$[1],"Equipamentos para Cabeça","Weapons","Other";
	setarray .Shops$[1],"Quest Menu";
// -----------------------------------------------------------
// Itens de missão - NÃO use um item de recompensa mais de uma vez!
// Adicionar (<loja ID>, <recompensa ID>, <quantidade recompensa>, <Custo Zeny>, <ponto de custo>,
//<ID item necessário>, <quantidade item necessário> {...});
// -----------------------------------------------------------


    if($ItemCostumeID == 5230){
		AddQ(1,5230,1,0,0,607,5,604,1);
	}else if($ItemCostumeID == 5231){
		AddQ(1,5231,1,0,0,607,5,604,1);
	}else if($ItemCostumeID == 5232){
		AddQ(1,5232,1,0,0,607,5,604,1);
	}else if($ItemCostumeID == 5233){
		AddQ(1,5233,1,0,0,607,5,604,1);
	}
		

// -----------------------------------------------------------

	freeloop(0);
	set .menu$,"";
	for(set .@i,1; .@i<=getarraysize(.Shops$); set .@i,.@i+1) {
		set .menu$, .menu$+.Shops$[.@i]+":";
		npcshopdelitem "qshopQ"+.@i,909;
	}
	end;

OnMenu:
	set .@size, getarraysize(@i);
	if (!.@size) set .@i, select(.menu$);
	else if (.@size == 1) set .@i, @i[0];
	else {
		for(set .@j,0; .@j<.@size; set .@j,.@j+1)
			set .@menu$, .@menu$+.Shops$[@i[.@j]]+":";
		set .@i, @i[select(.@menu$)-1];
	}
	deletearray @i[0],getarraysize(@i);
	if (.Shops$[.@i] == "") {
		message strcharinfo(0),"An error has occurred.";
		end;
	}
	 if($language == 1) {
		dispbottom "Selecione um item de cada vez.";
	  }else{
		dispbottom "Select one item at a time.";
	 }
	
	callshop "qshopQ"+.@i,1;
	npcshopattach "qshopQ"+.@i;
	end;

OnBuyItem:
	// .@q[] : RewardID, BoughtAmt, RewardAmt, BaseAmt, ReqZeny, ReqPts, { ReqItem, ReqAmt, ... }
	setarray .@q[0],@bought_nameid[0],((@bought_quantity[0] > .MaxStack)?.MaxStack:@bought_quantity[0]);
	copyarray .@q[3],getd(".q_"+.@q[0]+"[0]"),getarraysize(getd(".q_"+.@q[0]));
	set .@q[2],.@q[1]*.@q[3];
	if (!.@q[2] || .@q[2] > 30000) {
		message strcharinfo(0),"You can't purchase that many "+getitemname(.@q[0])+".";
		end;
	}
	
	if($language == 1) {
		mes "[Costume Clown]";
		mes "Recompensa: ^0055FFCostume's "+((.@q[2] > 1)?.@q[2]+"x ":"")+Slot(.@q[0])+"^000000";
		mes "Requerimento:";
	  }else{
		mes "[Costume Clown]";
		mes "Reward: ^0055FFCostume's "+((.@q[2] > 1)?.@q[2]+"x ":"")+Slot(.@q[0])+"^000000";
		mes "Required:";
	}
	
	if (.@q[4]) mes " > "+Chk(Zeny,.@q[4]*.@q[1])+(.@q[4]*.@q[1])+" Zeny^000000";
	if (.@q[5]) mes " > "+Chk(getd(.Points$[0]),.@q[5]*.@q[1])+(.@q[5]*.@q[1])+" "+.Points$[1]+" ("+getd(.Points$[0])+"/"+(.@q[5]*.@q[1])+")^000000";
	if (.@q[6]) for(set .@i,6; .@i<getarraysize(.@q); set .@i,.@i+2)
		mes " > "+Chk(countitem(.@q[.@i]),.@q[.@i+1]*.@q[1])+((.ShowID)?"{"+.@q[.@i]+"} ":"")+Slot(.@q[.@i])+" ("+countitem(.@q[.@i])+"/"+(.@q[.@i+1]*.@q[1])+")^000000";
	next;
	setarray @qe[1], getiteminfo(.@q[0],5), getiteminfo(.@q[0],11);
	if (@qe[2] > 0 && ((@qe[1] & 1) || (@qe[1] & 256) || (@qe[1] & 512) || (@qe[1] & 1024) || (@qe[1] & 2048) || (@qe[1] & 4096) || (@qe[1] & 4) || (@qe[1] & 8192)))
		set .@preview,1;
	addtimer 1000, strnpcinfo(0)+"::OnEnd";
	while(1) {
		switch(select(" ~ Convert ^0055FF"+getitemname(.@q[0])+"^000000: ~ ^777777Cancelar^000000")) {
		case 1:
		
	if($language == 1) {
		if (@qe[0]) {
				mes "[Costume Clown]";
				mes "Está faltando um ou mais itens.";
				close;
			}
			if (!checkweight(.@q[0],.@q[2])) {
				mes "[Costume Clown]";
				mes "^FF0000Você precisa "+(((.@q[2]*getiteminfo(.@q[0],6))+Weight-MaxWeight)/10)+" de capacidade de peso adicional para concluir este comércio.^000000";
				close;
			}
	  }else{
		if (@qe[0]) {
				mes "[Costume Clown]";
				mes "One or more items are missing.";
				close;
			}
			if (!checkweight(.@q[0],.@q[2])) {
				mes "[Costume Clown]";
				mes "^FF0000You need "+(((.@q[2]*getiteminfo(.@q[0],6))+Weight-MaxWeight)/10)+" of additional weight capacity to complete this trade.^000000";
				close;
			}
	 }	
			if (.@q[4]) Zeny -= (.@q[4]*.@q[1]);
			if (.@q[5]) setd .Points$[0], getd(.Points$[0])-(.@q[5]*.@q[1]);
			if (.@q[6]) for(set .@i,6; .@i<getarraysize(.@q); set .@i,.@i+2)
				delitem .@q[.@i],.@q[.@i+1]*.@q[1];
			//getitem .@q[0],.@q[2];
			costume $ItemCostumePart;
			specialeffect2 EF_FLOWERLEAF;
			close;
		case 2:
			close;
		}
	}

OnEnd:
	if (@qe[7]) {
		changelook LOOK_HEAD_BOTTOM, @qe[3];
		changelook LOOK_HEAD_TOP, @qe[4];
		changelook LOOK_HEAD_MID, @qe[5];
		changelook LOOK_ROBE, @qe[6];
	}
	deletearray @qe[0],8;
	end;

function AddQ {
	if (getitemname(getarg(1)) == "null") {
		debugmes "Quest reward #"+getarg(1)+" invalid (skipped).";
		return;
	}
	setarray .@j[0],getarg(2),getarg(3),getarg(4);
	for(set .@i,5; .@i<getargcount(); set .@i,.@i+2) {
		if (getitemname(getarg(.@i)) == "null") {
			debugmes "Quest requirement #"+getarg(.@i)+" invalid (skipped).";
			return;
		} else
			setarray .@j[.@i-2],getarg(.@i),getarg(.@i+1);
	}
	copyarray getd(".q_"+getarg(1)+"[0]"),.@j[0],getarraysize(.@j);
	npcshopadditem "qshopQ"+getarg(0),getarg(1),((.ShowZeny)?getarg(3):0);
	return;
}

function Chk {
	if (getarg(0) < getarg(1)) {
		set @qe[0],1;
		return "^FF0000";
	} else
		return "^0011FF";
}

function Slot {
	set .@s$,getitemname(getarg(0));
	switch(.ShowSlot) {
		case 1: if (!getitemslots(getarg(0))) return .@s$;
		case 2: if (getiteminfo(getarg(0),2) == 4 || getiteminfo(getarg(0),2) == 5) return .@s$+" ["+getitemslots(getarg(0))+"]";
		default: return .@s$;
	}
}

function A_An {
	setarray .@A$[0],"a","e","i","o","u";
	set .@B$, "_"+getarg(0);
	for(set .@i,0; .@i<5; set .@i,.@i+1)
		if (compare(.@B$,"_"+.@A$[.@i])) return "an "+getarg(0);
	return "a "+getarg(0);
}
}

function	script	qshopQ	{
	deletearray @i[0],getarraysize(@i);
	for(set .@i,0; .@i<getargcount(); set .@i,.@i+1)
		set @i[.@i],getarg(.@i);
	doevent "quest_shopQ::OnMenu";
	end;
}


// Dummy shop data -- copy as needed.
//============================================================
-	shop	qshopQ1	-1,909:-1
-	shop	qshopQ2	-1,909:-1
-	shop	qshopQ3	-1,909:-1
-	shop	qshopQ4	-1,909:-1
-	shop	qshopQ5	-1,909:-1